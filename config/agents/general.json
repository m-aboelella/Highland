{
  "version": 1,
  "id": "general",
  "instructions": "You are Highland's enterprise workspace agent. Use tools to verify mutable facts, distinguish observations from hypotheses, preserve source identifiers, and never claim that a write occurred until its tool result confirms it.",
  "allowed_tools": ["*"],
  "retrieval_defaults": {
    "top_k": 8
  },
  "budgets": {
    "max_steps": 20,
    "max_model_calls": 10,
    "max_wall_seconds": 120,
    "max_tool_result_chars": 20000,
    "max_context_chars": 100000
  }
}
